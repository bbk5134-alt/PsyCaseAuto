# Session 2B 작업 기록
**날짜:** 2026-03-29 (일요일)
**프로젝트:** PsyCaseAuto — WF1-A 디버깅 완료 + Telegram 알림 추가

---

## 완료된 작업

### 1. Netlify 배포 확인
- URL: `https://luxury-starburst-2deade.netlify.app`
- HTML 체크리스트 v2 정상 접근 확인
- localStorage에 Webhook URL/Secret 자동 저장 (재입력 불필요)

### 2. WF1-A 연속 디버깅 — 3단계 오류 해결

#### 오류 ①: `체크리스트 입력 없음` (Execution #127 이전)
- **원인:** 체크 항목 없이 전송 → 입력 검증 코드 정상 동작
- **해결:** 실제 체크 항목 입력 후 재전송

#### 오류 ②: `Invalid Value` — `location: "q"` (Execution #127)
- **원인:** `searchMethod: "name"` 모드에서 전체 Google Drive 쿼리문을 `queryString`에 넣음
  - `name` 모드의 `queryString` = 파일명 텍스트만 입력해야 함
  - `query` 모드의 `queryString` = 전체 Drive API 쿼리문 입력
- **수정:** 3개 Drive search 노드 모두 `searchMethod: "query"` 로 변경

#### 오류 ③: `File not found: .` — `location: "fileId"` (Execution #128, #134)
- **원인:** n8n Google Drive v3 노드의 `searchMethod: "query"` 버그
  - `files.list?q=...` 를 호출해야 하는데 `files.get(fileId=".")` 를 잘못 호출
  - `filter: {}` (비어있음) → driveId 미설정 → URL 빌드 시 `.` 로 처리됨
- **수정:** `searchMethod: "name"` + `filter.folderId` (mode: "id") 조합으로 변경
  - 환자 폴더 확인: `queryString={{ $json.raw.patient_code }}`, `folderId: 1awOFEQ8U2gjCdUWv6BXN3OGUe9CeeVv`
  - interviews 폴더 확인: `queryString="interviews"`, `folderId: {{ $json.folderId }}`
  - 기존 파일 조회: `queryString="interview_"`, `folderId: {{ $json.interviewsFolderId }}`

#### 오류 ④: Credential 오염 (Execution #133)
- **원인:** UI에서 credential 수동 변경 시도 → 존재하지 않는 ID `6CRBPbtmCW7AtsvJ` 로 오염
- **수정:** MCP로 6개 Google Drive 노드 credential 일괄 복원 → `fcKtWyui68XESQND`

### 3. WF1-A 최종 완성 확인
- **Execution #150:** Succeeded in 26ms ✅
- **Google Drive 결과:**
  ```
  PsyCaseAuto/
    └── PT-2026-001/
          └── interviews/
                └── interview_20260328.json  ← 생성 확인
  ```
- **HTML 체크리스트:** "전송 완료 (200) — n8n에서 워크플로우가 시작되었습니다." ✅

### 4. Telegram 완료 알림 노드 추가
- **노드명:** Telegram 알림
- **위치:** JSON 파일 저장 → (병렬 분기)
- **Credential:** `8T0Q83WRhTEEdvSL` (Telegram account 4)
- **Chat ID:** `7833910433`
- **메시지 형식 (HTML parse):**
  ```
  ✅ 면담 기록 저장 완료

  👤 환자: PT-2026-001
  📋 회차: #2
  📅 날짜: 2026-03-28
  👨‍⚕️ 면담자: R3 홍길동
  📁 파일: interview_20260328.json
  ☑️ 체크: 2/84개
  ⏰ 2026-03-29 02:20 KST
  ```
- **동작 확인:** Telegram 수신 ✅

---

## 최종 워크플로우 상태

| 항목 | 값 |
|------|-----|
| **ID** | `4BVLV65DL1FYqbAk` |
| **이름** | PsyCaseAuto — WF1-A 설문지 데이터 수신 (HTML + Form 이중) |
| **노드 수** | 20개 |
| **상태** | Published (활성) |
| **JSON 백업** | `n8n_workflows/wf1a_final_v2_20nodes.json` |

### 노드 목록 (최종)
| # | 노드명 | 타입 | 설명 |
|---|--------|------|------|
| 1 | Webhook | webhook v2 | HTML 체크리스트 수신 |
| 2 | Form Trigger | formTrigger v2.2 | n8n Form 백업 입력 |
| 3 | 입력 소스 통일 | code v2 | 두 소스 → 공통 구조화 |
| 4 | 시크릿 검증 | code v2 | X-Webhook-Secret 검증 |
| 5 | 입력 검증 | code v2 | 환자코드·날짜·체크리스트 유효성 |
| 6 | 환자 폴더 존재 확인 | googleDrive v3 | searchMethod:name + folderId filter |
| 7 | 폴더 없으면 생성? | if v2 | id == '' 분기 |
| 8 | 환자 폴더 생성 | googleDrive v3 | 루트 아래 환자 폴더 생성 |
| 9 | 폴더 ID 확정 | code v2 | 양 경로 합류 |
| 10 | interviews 서브폴더 조회 | googleDrive v3 | searchMethod:name + folderId filter |
| 11 | interviews 없으면 생성? | if v2 | 동일 패턴 |
| 12 | interviews 폴더 생성 | googleDrive v3 | 환자 폴더 아래 생성 |
| 13 | interviews ID 확정 | code v2 | 양 경로 합류 |
| 14 | 기존 파일 조회 | googleDrive v3 | searchMethod:name + folderId filter |
| 15 | JSON 직렬화 | code v2 | 면담 데이터 JSON 구조화 + 회차 계산 |
| 16 | JSON 파일 저장 | googleDrive v3 | interviews 폴더에 업로드 |
| 17 | 소스 분기 | if v2 | html_checklist vs n8n_form |
| 18 | Webhook 응답 | respondToWebhook v1.1 | HTML 경로 200 응답 |
| 19 | 폼 완료 | form v2.5 | Form 경로 완료 메시지 |
| 20 | Telegram 알림 | telegram v1.2 | 저장 완료 알림 (병렬) |

---

## 핵심 학습: Google Drive v3 Search 노드 올바른 사용법

```
❌ 잘못된 방식:
  searchMethod: "query"
  queryString: "name = 'PT-2026-001' and '...' in parents and ..."
  → n8n 버그: files.get(fileId=".") 호출 → 404

✅ 올바른 방식:
  searchMethod: "name"
  queryString: "PT-2026-001"  (파일명만)
  filter.folderId: { __rl: true, value: "FOLDER_ID", mode: "id" }
  → n8n이 내부적으로 name contains '...' 쿼리 생성 + 부모 폴더 필터 적용
```

---

## 다음 세션 예정 (Session 3)

- [ ] WF1-B: 녹음 파일 처리 / STT 변환 워크플로우 설계
- [ ] WF2: 보고서 생성 워크플로우 설계
- [ ] 실제 임상 사용 테스트 (여러 환자 코드)
- [ ] 동일 날짜 재면담 시 파일 덮어쓰기 처리 검토

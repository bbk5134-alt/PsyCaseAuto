# Session 2A 작업 기록
**날짜:** 2026-03-28 (토요일)
**프로젝트:** PsyCaseAuto — WF1-A 설문지 면담 저장

---

## 완료된 작업

### WF1-A 워크플로우 등록
- **Railway n8n ID:** `Tj0ScafoDJtKDiMA`
- **이름:** PsyCaseAuto — WF1-A 설문지 면담 저장
- **JSON 백업:** `n8n_workflows/wf1a_form_interview.json`
- **노드 수:** 14개
- **상태:** 비활성 (활성화 전 테스트 필요)
- **폼 URL (활성화 후):** `https://primary-production-b48fc.up.railway.app/form/psycaseauto-wf1a-interview-form`

### 노드 구성

| # | 노드명 | 타입 | 설명 |
|---|--------|------|------|
| 1 | Form Trigger | formTrigger v2.2 | 18개 필드 폼 |
| 2 | 입력 검증 | code v2 | PT-YYYY-NNN 형식, 필수값, 나이 범위 |
| 3 | 데이터 정규화 | code v2 | JSON 구조 생성, 파일명 계산 |
| 4 | 환자 폴더 존재 확인 | googleDrive v3 search | alwaysOutputData: true |
| 5 | 폴더 없으면 생성? | if v2 | $json.id ?? '' === '' |
| 6 | 환자 폴더 생성 | googleDrive v3 create | true 경로 |
| 7 | 폴더 ID 확정 | code v2 | 양 경로 합류점 |
| 8 | interviews 서브폴더 조회 | googleDrive v3 search | alwaysOutputData: true |
| 9 | interviews 없으면 생성? | if v2 | 동일 패턴 |
| 10 | interviews 폴더 생성 | googleDrive v3 create | true 경로 |
| 11 | interviews ID 확정 | code v2 | 양 경로 합류점 |
| 12 | JSON to Binary 변환 | code v2 | Buffer → base64 |
| 13 | JSON 파일 저장 | googleDrive v3 upload | interviews 폴더 안 |
| 14 | 완료 응답 | respondToWebhook v1.1 | 폼 제출자에게 응답 |

### Drive 저장 구조
```
PsyCaseAuto/ (루트, ID: 1awOFEQ8U2gjCdUWv6BXN3OGUe9CeeVv)
  └── {patient_code}/           ← 환자 폴더 (없으면 자동 생성)
        └── interviews/         ← 서브폴더 (없으면 자동 생성)
              └── form_{YYYYMMDD}.json  ← 면담 데이터
```

### 설계 결정 사항

**"폴더 없으면 생성" 패턴 구현 방식:**
- Google Drive search 노드에 `alwaysOutputData: true` 적용
  - 검색 결과 없을 때: `{}` 출력 → IF `$json.id ?? '' === ''` = true → 폴더 생성
  - 검색 결과 있을 때: `{id: "...", ...}` 출력 → IF false → 기존 폴더 사용
- IF true 경로 → 폴더 생성 → ID 확정 노드
- IF false 경로 → 직접 ID 확정 노드 (merge 없이 동일 노드로 연결)

---

## 다음 단계: 활성화 전 테스트 절차

1. Railway n8n → 워크플로우 열기 (`Tj0ScafoDJtKDiMA`)
2. Google Drive 노드 4개 Credential 확인 (`fcKtWyui68XESQND`)
3. 워크플로우 활성화 (토글 ON)
4. 폼 URL 접속: `https://primary-production-b48fc.up.railway.app/form/psycaseauto-wf1a-interview-form`
5. 테스트 데이터 입력:
   - patient_code: `PT-2024-001`
   - interview_date: `2024-01-15`
   - interview_number: `1`
   - interviewer: `R1`
   - sex: `M`
   - age: `35`
   - chief_problems: `테스트 주호소`
   - present_illness_notes: `테스트 현병력`
6. Google Drive 확인: `PsyCaseAuto/PT-2024-001/interviews/form_20240115.json`

---

## 이번 세션에서 하지 않은 것 (다음 세션)
- WF1-B: 녹음 파일 처리 / STT 변환
- WF2: 보고서 생성
- 실제 제출 테스트 (활성화 후 진행)
